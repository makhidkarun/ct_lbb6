[supervisord]
nodaemon=true

[program:nginx]
command=/usr/sbin/nginx
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0

[program:gunicorn]
environment=FLASK_CONFIG=development
command=/usr/bin/gunicorn manage:app -b localhost:5000 --access-logfile -
directory=/deploy
